# Диспетчерская служба аэропорта

Desktop-приложение для диспетчерской службы аэропорта. Оно помогает вести справочники авиакомпаний и аэропортов, формировать расписание и контролировать данные рейсов. Пользователь вводит данные в форме, приложение проверяет их, сохраняет в SQLite и отображает актуальные записи в таблицах интерфейса.

## Технологии

- Python 3
- Tkinter/ttk — графический пользовательский интерфейс
- SQLite — встроенная реляционная база данных
- `sqlite3` — подключение приложения к базе данных

## Реализовано

- автоматическое создание базы данных `airport.db`;
- создание таблиц авиакомпаний, аэропортов, самолётов и рейсов;
- начальное заполнение базы тестовыми данными;
- примеры рейсов для демонстрации диспетчерского расписания;
- просмотр списка авиакомпаний;
- добавление, изменение и удаление авиакомпаний с проверкой IATA-кода;
- просмотр списка аэропортов;
- добавление, изменение и удаление аэропортов с проверкой IATA-кода;
- управление самолётами: модель, регистрационный номер и количество мест;
- создание и просмотр рейсов с выбором авиакомпании, самолёта, аэропортов и статуса;
- изменение и удаление рейсов, отдельное обновление статуса;
- поиск и фильтрация рейсов по статусу;
- запрет дублирования номеров рейсов;
- формирование отчетов по расписанию, справочникам и статусам рейсов;
- сортировка таблиц по заголовкам столбцов;
- предварительный просмотр отчетов, выбор принтера Windows и очистка временных
  файлов после печати;
- проверка времени и запрет маршрута между одинаковыми аэропортами;
- внешние ключи и ограничения целостности данных;
- обработка ошибок при нарушении ограничений базы данных.
- интерактивная справка диспетчера с поиском по темам, кнопкой «Справка» и клавишей F1;
- схема базы данных нормализована минимум до третьей нормальной формы:
  города, модели самолётов и статусы рейсов хранятся в отдельных справочниках.

## Запуск проекта

1. Клонируйте репозиторий и перейдите в его каталог.
2. Создайте виртуальное окружение:

   ```powershell
   python -m venv .venv
   ```

3. Активируйте его в Windows:

   ```powershell
   .\.venv\Scripts\Activate.ps1
   ```

4. Запустите приложение:

   ```powershell
   python main.py
   ```

При первом запуске рядом с `main.py` автоматически создаётся файл `airport.db`.

## Вход в приложение

Перед открытием диспетчерской службы отображается форма авторизации.
Данные для входа по умолчанию:

- логин: `admin`;
- пароль: `admin`.

## Интерактивная справка

Нажмите кнопку «Справка» или клавишу **F1**. В окне справки можно выбрать тему,
получить подсказки для текущего раздела диспетчерской службы и найти нужную информацию по ключевым словам.
Окно закрывается клавишей **Esc**.

## Структура базы данных

Основные таблицы:

- `airlines` — авиакомпании;
- `cities` — города;
- `airports` — аэропорты и ссылки на города;
- `aircraft_models` — модели самолётов;
- `aircrafts` — самолёты и ссылки на модели;
- `flight_statuses` — статусы рейсов;
- `flights` — рейсы и внешние ключи на справочники.

## Структура проекта

```text
AirportMs/
├── database.py          # подключение, запросы и начальное заполнение SQLite
├── config.py            # настройки приложения, цвета и учетные данные по умолчанию
├── login_window.py      # окно авторизации
├── main.py              # orchestration главного окна
├── reports.py           # отчеты, сортировка и печать
├── help_window.py       # интерактивная справка
├── tabs/
│   ├── airlines_tab.py  # вкладка авиакомпаний
│   ├── airports_tab.py  # вкладка аэропортов
│   ├── aircrafts_tab.py # вкладка самолётов
│   ├── flights_tab.py   # вкладка рейсов
│   └── common.py        # общая таблица и сортировка
├── tests/
│   └── test_app.py      # автоматические тесты основных сценариев
├── requirements.txt     # зависимости проекта
└── README.md            # описание проекта
```

## Запуск тестов

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

## Работа с данными

- Для изменения выберите строку, проверьте данные в форме и нажмите «Изменить».
- Перед удалением приложение запрашивает явное подтверждение.
- Справочники, используемые рейсами, удалить нельзя: SQLite покажет понятное
  сообщение об ограничении внешнего ключа.
- Вкладка «Рейсы» поддерживает поиск по номеру, авиакомпании и кодам аэропортов,
  фильтр по статусу и отдельное обновление статуса.

## Отчеты и печать

Кнопка «Предпросмотр и печать» показывает текст отчета, позволяет выбрать
установленный принтер Windows и отправляет отчет на печать. Временный файл
удаляется после завершения процесса печати.

## Работа с базой данных

Все операции SQLite сосредоточены в `database.py`: вкладки и отчеты используют
именованные функции репозитория, параметризованные запросы и единый тип
`DatabaseError`. При запуске схема и индексы создаются идемпотентно, поэтому
обновление существующего `airport.db` не требует ручных действий.

## План развития

- добавить пассажиров, билеты и сотрудников;
- реализовать отчёты по расписанию и загрузке рейсов;
- добавить роли пользователей и авторизацию.
